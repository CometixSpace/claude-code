libcometix-asr — prebuilt napi addon

  source   https://github.com/Haleclipse/libcometix-asr
  commit   0930548  "Cryptify WSS Origin header to fix release string scan."
  built    2026-08-01  CI run 30722756475 (Build release bundles)
  sha256   verified against the .sha256 published beside each platform bundle

Taken from CI rather than a local build: the checked-in dist/ of the source
tree was six weeks stale (2026-07-16), predating the WSS live-path and Origin
header fixes, and a local build only produces the host platform.
