{
  "name": "cometix-patcher",
  "version": "0.1.0",
  "private": true,
  "description": "Opt-in patches for an installed @cometix/claude-code, applied in one scan and undone byte-for-byte",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node bin/patch.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "acorn": "^8.18.0",
    "ink": "^6.8.0",
    "react": "^19.3.0",
    "semver": "^7.8.5"
  },
  "devDependencies": {
    "ink-testing-library": "^4.0.0"
  }
}
